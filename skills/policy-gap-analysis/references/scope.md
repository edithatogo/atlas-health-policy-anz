# Scope mode

Require a target corpus, one or more comparator corpora, an observation date, and optional analytical frameworks. Freeze exact release/manifests before comparison. Record missing corpus coverage as a limitation rather than silently shrinking the denominator.
