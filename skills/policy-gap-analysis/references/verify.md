# Verify mode

Resolve pinned code, dataset, framework and model manifests. Rebuild from the earliest available authoritative layer, rerun the analysis and compare content hashes, counts, benchmark metrics and finding identities. Differences must be explained, not averaged away.
