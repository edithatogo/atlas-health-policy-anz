# Compare mode

Apply deterministic comparability gates first: identity, jurisdiction, authority, scope and time. Then use the smallest method that answers the open question: exact/rule match, lexical retrieval, ontology/concept mapping, embedding/cross-encoder/NLI, then a schema-constrained model if necessary. Preserve method disagreement.
