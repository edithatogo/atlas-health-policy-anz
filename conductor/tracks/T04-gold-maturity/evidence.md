# T04 Pre-gate evidence

T04 remains **planned**. The shadow clinical-governance slice contains 48 concept-level assertion candidates across the nine reference sources. Every candidate is A2/A3, carries `no_bronze_exact_span`, and is stamped `not_a_medallion_release`.

This is useful for concept ontology, framework mapping and GraphRAG design, but it is deliberately insufficient for Gold production because the source spans are source-grounded summaries rather than reversible exact Bronze→Silver lineage.
